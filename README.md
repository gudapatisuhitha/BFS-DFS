# BFS-DFS
BFS and DFS are graph traversal algorithms in Artificial Intelligence. BFS explores nodes level by level using a queue, while DFS explores deeply before backtracking using a stack or recursion. Both are widely used in searching, path finding, and problem-solving applications.
